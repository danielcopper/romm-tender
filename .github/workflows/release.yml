name: Release

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

# Serialize releases: two quick pushes to main must not run release-please
# concurrently and race on the release PR / tag.
concurrency:
  group: release
  cancel-in-progress: false

jobs:
  release-please:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    outputs:
      release_created: ${{ steps.release.outputs.release_created }}
      version: ${{ steps.release.outputs.version }}
      tag_name: ${{ steps.release.outputs.tag_name }}
    steps:
      # Mint a GitHub App installation token so release-please's PR/branch
      # pushes are authored by the App, not GITHUB_TOKEN. Pushes made with
      # GITHUB_TOKEN do not trigger workflows (anti-recursion), which left
      # the release PR's required status checks stuck "Expected" and
      # unmergeable. An App token makes those checks run automatically.
      # Scope it to exactly what release-please needs (contents + PRs).
      - uses: actions/create-github-app-token@v3
        id: app-token
        with:
          client-id: ${{ secrets.RELEASE_PLEASE_APP_CLIENT_ID }}
          private-key: ${{ secrets.RELEASE_PLEASE_APP_PRIVATE_KEY }}
          permission-contents: write
          permission-pull-requests: write

      - uses: googleapis/release-please-action@45996ed1f6d02564a971a2fa1b5860e934307cf7 # v5
        id: release
        with:
          token: ${{ steps.app-token.outputs.token }}
          config-file: release-please-config.json
          manifest-file: .release-please-manifest.json

  build-plugin:
    needs: release-please
    if: ${{ needs.release-please.outputs.release_created || github.event_name == 'workflow_dispatch' }}
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup Node
        uses: actions/setup-node@v7
        with:
          node-version: "24"

      - name: Setup pnpm
        uses: pnpm/action-setup@0977fd99725f1db4007ccb2928dbb4e90d06cc86 # v6

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Download Decky CLI
        run: |
          mkdir -p /tmp/decky-cli
          curl -L -o /tmp/decky-cli/decky "https://github.com/SteamDeckHomebrew/cli/releases/download/0.0.8/decky-linux-x86_64"
          # Pin the Decky CLI binary by SHA-256: it runs under sudo to build the
          # zip every user installs, so it is the pipeline's highest-leverage
          # supply-chain link. The hash is GitHub's own asset digest for tag 0.0.8
          # (gh api repos/SteamDeckHomebrew/cli/releases). A re-tagged or swapped
          # asset now fails the build instead of poisoning a release.
          echo "6777e356508c1ce887f8e61b0fa4954bb48b32e6d97341eef68ea4e0d6ead9a0  /tmp/decky-cli/decky" | sha256sum -c -
          chmod +x /tmp/decky-cli/decky
          echo "/tmp/decky-cli" >> $GITHUB_PATH

      - name: Package plugin
        run: |
          sudo $(which decky) plugin build -o /tmp/output -s directory $GITHUB_WORKSPACE
          sudo chown -R $(whoami) .

      - name: Determine tag name
        id: tag
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAG_NAME: ${{ needs.release-please.outputs.tag_name }}
        run: |
          if [ -n "$TAG_NAME" ]; then
            echo "tag=$TAG_NAME" >> "$GITHUB_OUTPUT"
          else
            echo "tag=$(gh release list --limit 1 --json tagName -q '.[0].tagName')" >> "$GITHUB_OUTPUT"
          fi

      - name: Smoke-test release zip
        run: |
          BUILT_ZIP=$(ls /tmp/output/*.zip | head -1)
          echo "Checking $BUILT_ZIP for required entries..."
          # bootstrap RAISES without 001_initial.sql (plugin fully inert).
          # Emulator resolution reads the live es_systems.xml directly (#1210) and
          # firmware requirements come off the installed cores through the vendored
          # resolver, so no snapshot of either ships in defaults/.
          # py_modules/native/libgavel-x86_64-linux.so is the compiled save-sync 409 kernel;
          # bootstrap RAISES (no Python fallback) if it's missing, so a Decky CLI
          # packaging change that drops it must fail the release here.
          # All three bootstrap modules are listed, not just the package marker:
          # __init__.py is a re-export shim, so asserting it alone would pass a
          # packaging regression that dropped the composition root's actual code.
          # py_modules/_vendor/atlas is the vendored emu-atlas resolver — the same
          # marker-vs-payload split applies, so a data/ file is listed beside the
          # package marker: the tree's JSON is half of what the package IS, and a
          # packaging change that carried only *.py would drop it in silence.
          for required in main.py plugin.json package.json dist/index.js bin/rom-launcher \
              py_modules/bootstrap/__init__.py py_modules/bootstrap/adapters.py py_modules/bootstrap/services.py \
              py_modules/db/migrations/001_initial.sql py_modules/db/migrations/002_add_emulator_override.sql \
              py_modules/native/libgavel-x86_64-linux.so \
              py_modules/_vendor/atlas/__init__.py py_modules/_vendor/atlas/data/system_ids.json; do
            if ! unzip -l "$BUILT_ZIP" | grep -qE "/$required\b"; then
              echo "ERROR: release zip is missing $required"
              unzip -l "$BUILT_ZIP"
              exit 1
            fi
          done
          # bin/rom-launcher must stay executable — Steam launches it directly,
          # and a Decky CLI version bump could silently drop the exec bit.
          LAUNCHER_PERMS=$(unzip -Z "$BUILT_ZIP" | grep -E '/bin/rom-launcher$' | awk '{print $1}')
          if [[ "$LAUNCHER_PERMS" != *x* ]]; then
            echo "ERROR: bin/rom-launcher is not executable in the zip (perms: '$LAUNCHER_PERMS')"
            unzip -Z "$BUILT_ZIP" | grep -E '/bin/rom-launcher$' || true
            exit 1
          fi
          if unzip -l "$BUILT_ZIP" | grep -qE '\.map\b'; then
            echo "ERROR: release zip contains source maps — should have been stripped"
            exit 1
          fi
          echo "Smoke test passed."

      - name: Upload release asset
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAG: ${{ steps.tag.outputs.tag }}
        run: |
          # The asset takes its name from the file uploaded, and the Decky CLI
          # names its output after the package. Renaming therefore means copying
          # — into RUNNER_TEMP, because /tmp/output belongs to the container that
          # produced the zip and is not writable here.
          # The name must be exactly "Tender.zip", for the update path rather than
          # the install. decky_loader's installFromURL takes the plugin name from
          # the URL's last path segment, and PluginBrowser._install spends it on
          # its "was this already installed?" lookup — find_plugin_folder(), an
          # exact match against each installed plugin.json's "name", "Tender" here.
          # A lower-cased name misses, so the previous copy is never uninstalled:
          # the new zip unpacks over it, stale files survive, and "Tender" is
          # appended to Decky's pluginOrder again. The install itself still
          # succeeds — Install-from-URL sends no version, so the backend's default
          # of "dev" applies, and on that path _install re-reads the name out of
          # the zip's own plugin.json before unpacking.
          BUILT_ZIP=$(ls /tmp/output/*.zip | head -1)
          DEST="$RUNNER_TEMP/Tender.zip"
          cp "$BUILT_ZIP" "$DEST"
          gh release upload "$TAG" \
            "$DEST" \
            --clobber
