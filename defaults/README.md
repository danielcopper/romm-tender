# Packaged defaults

Reference data that ships with the program. The backend reads `config.json` from this directory by that name, so do not
move or rename it.

## `config.json` — in-tree default

The platform-slug map. It is maintained in this repo (not vendored) and carries no checksum gate.
